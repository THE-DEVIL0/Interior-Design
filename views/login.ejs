<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login | Radiant Interiors</title>
  <link href="/css/output.css" rel="stylesheet" />
</head>
<body class="bg-gray-900 min-h-screen flex items-center justify-center">
    <a href="/" 
   class="absolute top-4 left-4 bg-cyan-600 hover:bg-cyan-700 text-white text-sm font-medium py-2 px-4 rounded-lg shadow transition duration-200">
  ⬅ Home
</a>


  <div class="w-full max-w-md bg-gray-800 rounded-xl shadow-2xl p-8">
    <h2 class="text-3xl font-bold text-center text-white mb-6">Welcome Back</h2>

    <% if (error) { %>
        <div class="bg-red-500 text-white text-sm text-center py-2 rounded">
          <%= error %>
        </div>
      <% } %>

    <form action="/login" method="POST" class="space-y-5">
      <!-- Email -->
      <div>
        <label for="email" class="block text-sm font-medium text-gray-300">Email Address</label>
        <input type="email" name="email" id="email" required
          class="mt-1 block w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-md shadow-sm focus:ring focus:ring-cyan-500 focus:outline-none" />
      </div>

      <!-- Password -->
      <div>
        <label for="password" class="block text-sm font-medium text-gray-300">Password</label>
        <input type="password" name="password" id="password" required
          class="mt-1 block w-full px-4 py-2 bg-gray-700 text-white border border-gray-600 rounded-md shadow-sm focus:ring focus:ring-cyan-500 focus:outline-none" />
      </div>

      <!-- Submit Button -->
      <div>
        <button type="submit"
          class="w-full bg-cyan-600 hover:bg-cyan-700 text-white font-semibold py-2 px-4 rounded shadow transition duration-200">
          Sign In
        </button>
      </div>
    </form>

    <p class="text-center text-sm text-gray-400 mt-4">
      New to Radiant Interiors?
      <a href="/register" class="text-cyan-400 hover:underline font-medium">Create an account</a>
    </p>
  </div>

</body>
</html>
